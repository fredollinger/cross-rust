task :default => :build

desc "build for aarch64"
task :build do
    sh "cargo build --release --target=aarch64-unknown-linux-gnu --verbose"
end

desc "setup aarch64"
task :setup do
    puts "STUB NOT DONE"
    puts "see https://robotsfuckyeahalloneword.svbtle.com/crosscompiling-to-aarch64"
end
